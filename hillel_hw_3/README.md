# Simple Flask application with SQLite

### Project Endpoints

- `/names/`
   - Retrieve unique first names from customers.
- `/customers/`
   - Retrieve customers with optional filtering.
- `/tracks/count/`
   - Retrieve the count of tracks.
- `/tracks/duration/`
   - Retrieve track names and durations.